#include <stdio.h>
#include <stdlib.h>

#include "hashtable.h"

//initialize a hashtable with MAX(50) possible key value pairs
node table[MAX];

void test(void){
    printf("this works\n");
}

int tabulate(void){
    int x = 5;
    int y = 10;
    
    return x + y;
}